/**
 * FlixelGDX's own reflection-free JSON layer.
 *
 * <p>{@link org.flixelgdx.json.FlixelJson} parses text into a
 * {@link org.flixelgdx.json.FlixelJsonValue} tree with typed, default-friendly getters. Because
 * no reflection is involved, the same code runs unchanged on desktop, Android, web, and native
 * images without configuration files.
 */
package org.flixelgdx.json;
